name: Prepare merge

# Triggered when a maintainer applies the `ready-to-ship` label to an
# approved PR. Drives the per-package matrix flow against GitHub's PR
# merge ref (preflight + toolchain-cache + matrix-build + test-gate;
# Phase B-1's pattern, targeting the durable `binaries-abi-v<N>` tag),
# then posts `merge-gate=success` on the original PR head and enables
# auto-merge.
#
# Flow (see docs/plans/2026-05-05-decoupled-package-builds-design.md
# §5.5 + docs/plans/2026-04-29-pr-package-builds-design.md §4.2):
#   * targets the undated `binaries-abi-v<N>` tag (per-archive uploads),
#   * preflight runs against the PR merge ref and narrows the matrix to
#     `(package, arch)` pairs whose cache_key_sha differs from what's
#     already published in the durable release,
#   * builds each remaining pair in parallel via `xtask archive-stage`,
#     publishing each archive and index entry atomically, and
#   * gates the merge on the test-suite resolving from the durable
#     release state.
#
# `merge-gate=success` is posted on the original PR HEAD SHA after the
# durable-release test gate passes against the PR merge ref. Branch
# protection on `main` requires this status.
#
# See:
#   docs/plans/2026-05-05-decoupled-package-builds-design.md §5.5
#   docs/plans/2026-04-29-pr-package-builds-design.md §4.2

on:
  pull_request:
    types: [labeled]

# Do not use workflow-level concurrency. GitHub Actions keeps at most
# one pending run per concurrency group, so a third ready PR cancels/
# replaces the second instead of forming a real queue. The
# durable-release-lock (acquired by `merge-gate-finalize`) serializes
# durable-release publishing while preserving every workflow run.

jobs:
  # ---------------------------------------------------------------------
  # gate — review state + required-checks + wait-for-staging-build
  # when package staging is required.
  # All gates that must pass BEFORE we burn matrix-build compute live
  # here so a failed gate is cheap. On success, downstream jobs run.
  # ---------------------------------------------------------------------
  change-scope:
    if: |
      github.event.label.name == 'ready-to-ship' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      kernel: ${{ steps.scope.outputs.kernel }}
      docs_only: ${{ steps.scope.outputs.docs_only }}
      package_staging_required: ${{ steps.scope.outputs.package_staging_required }}
      package_staging_reason: ${{ steps.scope.outputs.package_staging_reason }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Detect package staging scope
        id: scope
        uses: ./.github/actions/detect-change-scope
        with:
          base-ref: origin/${{ github.event.pull_request.base.ref }}

  gate:
    needs: [change-scope]
    if: |
      github.event.label.name == 'ready-to-ship' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write     # drop label / comment on failure
      statuses: read
    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Verify PR review state + required checks green
        id: gate
        run: |
          # reviewDecision values:
          #   APPROVED          — at least one approving review, branch
          #                       protection requires reviews
          #   REVIEW_REQUIRED   — branch protection requires reviews,
          #                       none yet
          #   CHANGES_REQUESTED — at least one CHANGES_REQUESTED review
          #   ""/null           — no review policy on the branch (solo
          #                       maintainer flows, internal repos)
          #
          # Default policy: block REVIEW_REQUIRED and CHANGES_REQUESTED.
          # Maintainer override: a user with admin/maintain permission
          # who applies `ready-to-ship` can bypass the review gate (the
          # ask was: let maintainers ship without an approving review).
          # Required-check failures are NEVER bypassable here — those
          # gate on objective signals.
          LABELER="${{ github.event.sender.login }}"
          PERM=$(gh api "/repos/${{ github.repository }}/collaborators/${LABELER}/permission" -q .permission 2>/dev/null || echo "")
          MAINTAINER=0
          case "$PERM" in
            admin|maintain) MAINTAINER=1 ;;
          esac
          echo "labeler=$LABELER permission=$PERM maintainer=$MAINTAINER"

          REVIEW=$(gh pr view "$PR_NUMBER" --repo ${{ github.repository }} --json reviewDecision -q .reviewDecision)
          case "$REVIEW" in
            REVIEW_REQUIRED|CHANGES_REQUESTED)
              if [ "$MAINTAINER" = "1" ]; then
                echo "::notice::review state $REVIEW; bypassed by maintainer ${LABELER}"
              else
                gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
                gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: PR review state is ${REVIEW}. (A maintainer — admin/maintain permission — can re-apply \`ready-to-ship\` to override.)"
                exit 1
              fi
              ;;
          esac

          # Count failing required checks. Other non-skipping checks
          # currently in flight are tolerated. Required-check failure is
          # NOT bypassable, even by maintainers — the workflow's own
          # post-publish tests gate the merge-gate status, and bypassing
          # pre-existing failed checks here would defeat that intent.
          FAILING=$(gh pr checks "$PR_NUMBER" --repo ${{ github.repository }} --json bucket -q '[.[] | select(.bucket == "fail")] | length')
          if [ "$FAILING" -gt 0 ]; then
            gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: $FAILING required check(s) failing."
            exit 1
          fi

      - name: Validate PR is mergeable into main
        # Validate-only check so prepare-merge can post a clear message
        # and drop the label when GitHub cannot compose the PR merge
        # ref. The package matrix below checks out `github.ref`
        # (`refs/pull/<N>/merge` on pull_request events), so cache keys,
        # package builds, and tests run against the merged tree rather
        # than the PR head alone.
        #
        # Uses a shallow checkout + `git merge-tree` to test mergeability
        # without a working tree or any state side-effects. Conflict
        # markers in the merge-tree output indicate textual conflicts.
        run: |
          # Fetch enough history to merge-tree base + heads.
          git init --quiet /tmp/merge-check
          cd /tmp/merge-check
          git remote add origin "https://${{ github.token }}@github.com/${{ github.repository }}.git"
          git fetch --quiet origin "main:refs/remotes/origin/main"
          git fetch --quiet origin "$PR_HEAD_SHA"

          # `git merge-tree` (3-arg form) writes the merged tree to its
          # stdout and emits conflict markers (`<<<<<<<`) on textual
          # conflicts. Non-zero exit indicates an unrecoverable merge
          # (e.g. unrelated histories) — treat the same as a conflict.
          MERGE_BASE=$(git merge-base "refs/remotes/origin/main" "$PR_HEAD_SHA" || true)
          if [ -z "$MERGE_BASE" ]; then
            echo "::warning::Could not find merge-base; assuming conflict."
            CONFLICT=1
          else
            set +e
            output=$(git merge-tree "$MERGE_BASE" "refs/remotes/origin/main" "$PR_HEAD_SHA" 2>&1)
            mt_status=$?
            set -e
            if [ "$mt_status" -ne 0 ]; then
              echo "git merge-tree failed: $output"
              CONFLICT=1
            elif printf '%s\n' "$output" | grep -q '^<<<<<<<'; then
              CONFLICT=1
            else
              CONFLICT=0
            fi
          fi

          if [ "$CONFLICT" = "1" ]; then
            gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: PR has merge conflicts with main. Please rebase or merge main and re-apply the label."
            exit 1
          fi

          echo "PR $PR_HEAD_SHA merges cleanly into origin/main."

      - name: Wait for staging-build on PR HEAD
        if: needs.change-scope.outputs.package_staging_required == 'true'
        # staging-build runs the same matrix flow on every PR push (and
        # since Phase B-1, the matrix already targets the undated
        # `binaries-abi-v<N>` tag in parallel with the legacy build).
        # Waiting here lets prepare-merge promote PR-staged archives
        # whose cache key still matches the PR merge ref, while rebuilding
        # entries whose merged-tree inputs changed.
        run: |
          # The label event can fire before staging-build's run is
          # registered against PR_HEAD_SHA. Retry briefly.
          RUN_ID=""
          for attempt in 1 2 3 4 5 6; do
            RUN_ID=$(gh run list \
              --repo ${{ github.repository }} \
              --workflow=staging-build.yml \
              --commit="$PR_HEAD_SHA" \
              --limit=1 \
              --json databaseId \
              --jq '.[0].databaseId // empty')
            [ -n "$RUN_ID" ] && break
            sleep 10
          done

          if [ -z "$RUN_ID" ]; then
            gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: no \`staging-build\` run found for HEAD \`$PR_HEAD_SHA\` after 60s. Push a new commit (or close+reopen the PR) to trigger one, then re-apply \`ready-to-ship\`."
            exit 1
          fi

          echo "Waiting for staging-build run $RUN_ID on $PR_HEAD_SHA…"
          # --exit-status: non-zero on failure/cancelled conclusion.
          if ! gh run watch "$RUN_ID" --repo ${{ github.repository }} --exit-status; then
            gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: \`staging-build\` for HEAD \`$PR_HEAD_SHA\` did not succeed (run ${{ github.server_url }}/${{ github.repository }}/actions/runs/$RUN_ID). Re-apply \`ready-to-ship\` once it is green."
            exit 1
          fi

      - name: Drop label + comment on gate failure
        if: failure()
        run: |
          gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship || true
          gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge gate failed. Check the workflow run log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}. Re-apply the ready-to-ship label after fixing." || true

  # ---------------------------------------------------------------------
  # Per-package matrix flow.
  #
  # Mirrors `staging-build.yml`'s preflight + toolchain-cache +
  # matrix-build + test-gate + publish + generate-index, with one
  # adjustment: target_tag is always `binaries-abi-v<N>` (durable),
  # never `pr-<NNN>-staging`. The rest of the flow is structurally
  # identical, including the deterministic-archive guarantees the
  # matrix relies on.
  #
  # A future cleanup PR can extract this shape into a reusable
  # `workflow_call` workflow shared between staging-build and
  # prepare-merge. For the Phase C cutover we deliberately copy-paste
  # to keep the diff localized.
  # ---------------------------------------------------------------------

  preflight:
    needs: [gate, change-scope]
    if: needs.change-scope.outputs.package_staging_required == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read           # gh release view (target tag probe)
    outputs:
      library_matrix: ${{ steps.compute.outputs.library_matrix }}
      program_matrix: ${{ steps.compute.outputs.program_matrix }}
      promote_matrix: ${{ steps.compute.outputs.promote_matrix }}
      abi: ${{ steps.compute.outputs.abi }}
      target_tag: ${{ steps.compute.outputs.target_tag }}
      staging_tag: ${{ steps.compute.outputs.staging_tag }}
      merge_base_sha: ${{ steps.compute.outputs.merge_base_sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          # On pull_request events this is refs/pull/<N>/merge. Build
          # and test the exact tree GitHub intends to merge, not just
          # the PR head. fetch-depth 2 keeps the merge parent available
          # for changed-file detection and the final base-drift guard.
          ref: ${{ github.ref }}
          fetch-depth: 2

      - name: Fetch musl submodule
        # Hardened submodule fetch via the reusable action (PR #435).
        # preflight only needs musl (it builds xtask and computes the
        # matrix; tests/libc/libc-test and tests/sortix/os-test materialize in matrix-build / test-gate
        # via their own checkouts). The durable-release-lock and the
        # mergeability/change-scope gates from the legacy publish job
        # have moved to `gate` (`Validate PR is mergeable into main`)
        # and `merge-gate-finalize` (durable-release-lock acquire) per
        # the Phase C restructure.
        uses: ./.github/actions/fetch-submodules
        with:
          submodules: libc/musl

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22

      - name: Cache Nix store + flake eval
        uses: DeterminateSystems/magic-nix-cache-action@v13
        with:
          use-gha-cache: false

      - name: Warm Nix dev shell
        run: bash scripts/dev-shell.sh true

      - name: Cache xtask build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            target
          key: preflight-${{ runner.os }}-${{ hashFiles('Cargo.lock', 'tools/xtask/**') }}
          restore-keys: |
            preflight-${{ runner.os }}-

      - name: Build xtask
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo build --release -p xtask --target "$HOST_TARGET"
          '

      - name: Compute matrix
        id: compute
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bash scripts/dev-shell.sh bash -c '
            set -euo pipefail

            parents=$(git show --no-patch --format=%P HEAD)
            MERGE_BASE_SHA=$(printf "%s\n" "$parents" | awk "{print \$1}")
            MERGE_HEAD_SHA=$(printf "%s\n" "$parents" | awk "{print \$2}")
            if [ -z "$MERGE_BASE_SHA" ] || [ -z "$MERGE_HEAD_SHA" ]; then
              echo "::error::prepare-merge expected HEAD to be the pull_request merge commit, got parents: $parents"
              exit 1
            fi
            if [ "$MERGE_HEAD_SHA" != "${{ github.event.pull_request.head.sha }}" ]; then
              echo "::error::pull_request merge ref second parent $MERGE_HEAD_SHA does not match PR head ${{ github.event.pull_request.head.sha }}"
              exit 1
            fi
            echo "merge_base_sha=$MERGE_BASE_SHA" >> "$GITHUB_OUTPUT"

            if ! git cat-file -e "$MERGE_BASE_SHA^{commit}" 2>/dev/null || \
               ! git cat-file -e "$MERGE_HEAD_SHA^{commit}" 2>/dev/null; then
              git fetch --deepen=1 origin "${{ github.ref }}"
            fi

            pr_changed_files=$(git diff --name-only "$MERGE_BASE_SHA"..HEAD || true)
            base_delta_files=$(git diff --name-only "$MERGE_HEAD_SHA" "$MERGE_BASE_SHA" || true)
            changed_files=$(printf "%s\n%s\n" "$pr_changed_files" "$base_delta_files" | sed "/^$/d" | sort -u)
            force_package_rebuild=0
            if echo "$changed_files" | grep -E \
              -e "^crates/fork-instrument/" \
              -e "^scripts/build-fork-instrument-tool\\.sh$" \
              -e "^scripts/dev-shell\\.sh$" \
              -e "^(Cargo\\.(lock|toml)|flake\\.(nix|lock)|rust-toolchain\\.toml)$" \
              >/dev/null; then
              force_package_rebuild=1
              echo "preflight: forcing package matrix because fork-instrument/toolchain inputs changed"
            fi

            ABI=$(grep -oE "ABI_VERSION: u32 = [0-9]+" crates/shared/src/lib.rs | awk "{print \$4}")
            echo "abi=$ABI" >> "$GITHUB_OUTPUT"

            # prepare-merge always targets the durable, undated tag.
            # Distinct from staging-build, which uses pr-<NNN>-staging
            # for PR runs. The two flows can run in parallel without
            # conflict because their target tags differ.
            TARGET_TAG="binaries-abi-v${ABI}"
            echo "target_tag=$TARGET_TAG" >> "$GITHUB_OUTPUT"
            STAGING_TAG="pr-${{ github.event.pull_request.number }}-staging"
            echo "staging_tag=$STAGING_TAG" >> "$GITHUB_OUTPUT"

            existing_assets=$(gh release view "$TARGET_TAG" --repo "${{ github.repository }}" --json assets --jq "[.assets[].name]" 2>/dev/null || echo "[]")
            echo "existing-assets:"
            echo "$existing_assets" | jq -r ".[0:20][]"
            echo "..."
            existing_index_dir=$(mktemp -d)
            existing_index="$existing_index_dir/index.toml"
            if gh release download "$TARGET_TAG" --repo "${{ github.repository }}" --pattern index.toml --dir "$existing_index_dir" --clobber >/dev/null 2>&1; then
              echo "existing-index: downloaded $TARGET_TAG/index.toml"
            else
              echo "existing-index: none"
              : > "$existing_index"
            fi
            staging_assets=$(gh release view "$STAGING_TAG" --repo "${{ github.repository }}" --json assets --jq "[.assets[].name]" 2>/dev/null || echo "[]")
            echo "staging-assets:"
            echo "$staging_assets" | jq -r ".[0:20][]"
            echo "..."

            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            library_matrix="[]"
            program_matrix="[]"
            promote_matrix="[]"

            # Temporarily disabled packages — too slow to rebuild in
            # CI while we have no separate prebuilt-package repo to
            # offload them to. Each is still consumable via its
            # existing `[binary]` pin in package.toml; prepare-merge
            # just refuses to redo the work. Remove an entry here
            # (and mirror in staging-build.yml) to re-enable rebuilds.
            disabled_pkgs=" cpython erlang erlang-vfs perl perl-vfs python-vfs redis ruby texlive "

            for pkg_dir in packages/registry/*/; do
              pkg=$(basename "$pkg_dir")
              if ! grep -q "^\[build\]" "$pkg_dir/package.toml"; then
                continue
              fi

              # Disabled-package skip (see `disabled_pkgs` above).
              if [[ "$disabled_pkgs" == *" $pkg "* ]]; then
                echo "preflight: skipping $pkg (temporarily disabled)" >&2
                continue
              fi

              kind=$(sed -nE "s/^kind *= *\"([^\"]+)\".*/\\1/p" "$pkg_dir/package.toml" | head -1)
              case "$kind" in
                library|program) ;;
                *) echo "preflight: skipping $pkg (kind=$kind)" >&2; continue ;;
              esac

              # Version comes from package.toml (recipe). Revision
              # comes from build.toml (project view); defaults to 1
              # when build.toml omits it.
              version=$(sed -nE "s/^version *= *\"([^\"]+)\".*/\\1/p" "$pkg_dir/package.toml" | head -1)
              revision=1
              if [ -f "$pkg_dir/build.toml" ]; then
                revision=$(sed -nE "s/^revision *= *([0-9]+).*/\\1/p" "$pkg_dir/build.toml" | head -1)
                revision=${revision:-1}
              fi
              arches=$(awk -F'"'"'[][]'"'"' '"'"'/^arches *=/ {print $2}'"'"' "$pkg_dir/package.toml" | tr -d '"'"' "'"'"' | tr '"'"','"'"' '"'"' '"'"')
              arches=${arches:-wasm32}
              for arch in $arches; do
                sha=$(cargo run --release -p xtask --target "$HOST_TARGET" --quiet -- \
                  compute-cache-key-sha --package "$pkg_dir" --arch "$arch" 2>/dev/null) || {
                  echo "preflight: skipping $pkg/$arch (compute-cache-key-sha failed)" >&2
                  continue
                }
                short=${sha:0:8}
                prefix="${pkg}-"
                suffix="-abi${ABI}-${arch}-${short}.tar.zst"
                existing_archive=$(echo "$existing_assets" | jq -r --arg pre "$prefix" --arg suf "$suffix" \
                    "first(.[] | select(startswith(\$pre) and endswith(\$suf))) // \"\"")
                if [ "$force_package_rebuild" != "1" ] && [ -n "$existing_archive" ] && scripts/index-has-current-entry.sh "$existing_index" "$pkg" "$arch" "$sha" "$revision"; then
                  echo "preflight: skip $pkg/$arch (sha $short already indexed)"
                  continue
                fi
                entry=$(jq -nc --arg pkg "$pkg" --arg arch "$arch" --arg sha "$sha" \
                                --arg ver "$version" --argjson rev "$revision" \
                  "{package: \$pkg, arch: \$arch, sha: \$sha, version: \$ver, revision: \$rev}")
                staging_archive=$(echo "$staging_assets" | jq -r --arg pre "$prefix" --arg suf "$suffix" \
                    "first(.[] | select(startswith(\$pre) and endswith(\$suf))) // \"\"")
                if [ "$force_package_rebuild" != "1" ] && [ -n "$staging_archive" ]; then
                  promote_entry=$(echo "$entry" | jq -c --arg archive "$staging_archive" ". + {archive_name: \$archive}")
                  promote_matrix=$(echo "$promote_matrix" | jq -c --argjson e "$promote_entry" ". + [\$e]")
                  echo "preflight: promote $pkg/$arch (sha $short from $STAGING_TAG)"
                  continue
                fi
                if [ "$kind" = "library" ]; then
                  library_matrix=$(echo "$library_matrix" | jq -c --argjson e "$entry" ". + [\$e]")
                else
                  program_matrix=$(echo "$program_matrix" | jq -c --argjson e "$entry" ". + [\$e]")
                fi
              done
            done

            echo "library_matrix=$library_matrix" >> "$GITHUB_OUTPUT"
            echo "program_matrix=$program_matrix" >> "$GITHUB_OUTPUT"
            echo "promote_matrix=$promote_matrix" >> "$GITHUB_OUTPUT"
            echo "preflight: promote=$(echo "$promote_matrix" | jq length) library-build=$(echo "$library_matrix" | jq length) program-build=$(echo "$program_matrix" | jq length)"
            echo "promote_matrix:"; echo "$promote_matrix" | jq .
            echo "library_matrix:"; echo "$library_matrix" | jq .
            echo "program_matrix:"; echo "$program_matrix" | jq .
          '

  toolchain-cache:
    needs: [gate, change-scope]
    if: |
      needs.change-scope.outputs.package_staging_required == 'true' ||
      needs.change-scope.outputs.kernel == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Fetch musl submodule
        uses: ./.github/actions/fetch-submodules
        with:
          submodules: libc/musl

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v18

      - name: Cache Nix store + flake eval
        uses: DeterminateSystems/magic-nix-cache-action@v13

      - name: Compute cache key
        # Mirrors `staging-build.yml`'s `toolchain-cache.key` formula
        # exactly so a hit on either workflow's cache warms both.
        id: key
        run: |
          KEY="musl-sysroot-v4-${{ runner.os }}-${{ hashFiles('scripts/build-musl.sh', 'packages/registry/libcxx/**', 'libc/musl-overlay/**', 'libc/glue/**') }}"
          echo "value=$KEY" >> "$GITHUB_OUTPUT"
          echo "toolchain-cache key: $KEY"

      - name: Cache musl sysroot + libc++
        uses: actions/cache@v4
        id: cache
        with:
          path: |
            sysroot
            sysroot64
          key: ${{ steps.key.outputs.value }}

      - name: Build sysroots if cache miss
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          bash scripts/dev-shell.sh bash scripts/build-musl.sh
          bash scripts/dev-shell.sh bash scripts/build-musl.sh --arch wasm64posix
          bash scripts/dev-shell.sh bash -c '
            set -euo pipefail
            HOST_TARGET="$(rustc -vV | awk "/^host/ {print \$2}")"
            for arch in wasm32 wasm64; do
              cargo run -p xtask --target "$HOST_TARGET" --quiet -- \
                build-deps resolve libcxx --arch "$arch"
              prefix="$(cargo run -p xtask --target "$HOST_TARGET" --quiet -- \
                build-deps path libcxx --arch "$arch")"
              if [ "$arch" = "wasm64" ]; then sysroot="sysroot64"; else sysroot="sysroot"; fi
              mkdir -p "$sysroot/lib" "$sysroot/include/c++"
              ln -sf "$prefix/lib/libc++.a"    "$sysroot/lib/libc++.a"
              ln -sf "$prefix/lib/libc++abi.a" "$sysroot/lib/libc++abi.a"
              rm -rf "$sysroot/include/c++/v1"
              ln -sfn "$prefix/include/c++/v1" "$sysroot/include/c++/v1"
            done
          '

      - name: Build fork-instrument host tool
        run: bash scripts/dev-shell.sh bash scripts/build-fork-instrument-tool.sh

      - name: Pack toolchain sysroots + host tools
        run: |
          tar --zstd -cf "$RUNNER_TEMP/toolchain-sysroots.tar.zst" \
            sysroot sysroot64 tools/bin/wasm-fork-instrument

      - name: Upload toolchain sysroots artifact
        uses: actions/upload-artifact@v4
        with:
          name: toolchain-sysroots
          path: ${{ runner.temp }}/toolchain-sysroots.tar.zst
          retention-days: 1
          if-no-files-found: error

  # Fast path for prepare-merge: if no force rebuild is needed and
  # staging already built the exact cache-keyed archive for the merged
  # tree, copy that asset into the durable release and update
  # binaries-abi-v<N>/index.toml under the same state-lock used by
  # regular matrix publishes. Fall back to lib/program matrix builds
  # for forced entries and entries absent from both durable and PR
  # staging releases.
  promote-staging:
    needs: [preflight]
    if: needs.preflight.outputs.promote_matrix != '[]'
    runs-on: ubuntu-latest
    permissions:
      contents: write    # gh release download/upload + state-lock git push
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        include: ${{ fromJSON(needs.preflight.outputs.promote_matrix) }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Build xtask
        run: |
          HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
          cargo build --release -p xtask --target "$HOST_TARGET"

      - name: Download staged archive
        id: archive
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/promote"
          gh release download "${{ needs.preflight.outputs.staging_tag }}" \
            --repo "$GITHUB_REPOSITORY" \
            --pattern "${{ matrix.archive_name }}" \
            --dir "$RUNNER_TEMP/promote" \
            --clobber
          archive_path="$RUNNER_TEMP/promote/${{ matrix.archive_name }}"
          if [ ! -f "$archive_path" ]; then
            echo "::error::staged archive not downloaded: $archive_path"
            exit 1
          fi
          echo "archive-path=$archive_path" >> "$GITHUB_OUTPUT"
          echo "archive-name=${{ matrix.archive_name }}" >> "$GITHUB_OUTPUT"

      - name: Promote archive + index entry
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          bash scripts/index-update.sh \
            --target-tag "${{ needs.preflight.outputs.target_tag }}" \
            --package "${{ matrix.package }}" \
            --version "${{ matrix.version }}" \
            --revision "${{ matrix.revision }}" \
            --arch "${{ matrix.arch }}" \
            --status success \
            --archive-path "${{ steps.archive.outputs.archive-path }}" \
            --archive-name "${{ steps.archive.outputs.archive-name }}" \
            --cache-key-sha "${{ matrix.sha }}"

  # Library wave — kind=library packages. See staging-build.yml for
  # the lib-then-program design rationale.
  lib-matrix-build:
    needs: [preflight, toolchain-cache]
    if: ${{ needs.preflight.result == 'success' && needs.toolchain-cache.result == 'success' && needs.preflight.outputs.library_matrix != '[]' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write    # state-lock git push + gh release create/upload
    env:
      WASM_POSIX_BINARY_INDEX_URL: https://github.com/${{ github.repository }}/releases/download/${{ needs.preflight.outputs.target_tag }}/index.toml
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        include: ${{ fromJSON(needs.preflight.outputs.library_matrix) }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Compute commit-bound build provenance
        id: provenance
        run: |
          ts=$(git log -1 --format=%aI HEAD)
          echo "build-timestamp=$ts" >> "$GITHUB_OUTPUT"
          echo "build-host=${{ github.repository }}@${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Fetch musl submodule
        uses: ./.github/actions/fetch-submodules
        with:
          submodules: libc/musl

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22

      - name: Cache Nix store + flake eval
        uses: DeterminateSystems/magic-nix-cache-action@v13

      - name: Download toolchain sysroots artifact
        uses: actions/download-artifact@v4
        with:
          name: toolchain-sysroots
          path: ${{ runner.temp }}/toolchain

      - name: Extract toolchain sysroots
        run: |
          tar --zstd -xf "$RUNNER_TEMP/toolchain/toolchain-sysroots.tar.zst"

      - name: Install host npm deps (build prereq)
        run: |
          bash scripts/dev-shell.sh bash -c '
            PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
            cd host && PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
          '

      - name: Resolve target package's transitive deps
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo run --release -p xtask --target "$HOST_TARGET" --quiet -- \
              build-deps --arch "${{ matrix.arch }}" --binaries-dir "$(pwd)/binaries" \
                resolve "${{ matrix.package }}"
          '

      - name: Build ${{ matrix.package }} (${{ matrix.arch }})
        id: build
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo run --release -p xtask --target "$HOST_TARGET" --quiet -- \
              archive-stage \
                --package "packages/registry/${{ matrix.package }}" \
                --arch "${{ matrix.arch }}" \
                --out "$RUNNER_TEMP/staged" \
                --build-timestamp "${{ steps.provenance.outputs.build-timestamp }}" \
                --build-host "${{ steps.provenance.outputs.build-host }}"
          '
          archive_path=$(find "$RUNNER_TEMP/staged" -name '*.tar.zst' -print -quit)
          archive_name=$(basename "$archive_path")
          echo "archive-path=$archive_path" >> "$GITHUB_OUTPUT"
          echo "archive-name=$archive_name" >> "$GITHUB_OUTPUT"

      - name: Upload archive as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.package }}-${{ matrix.arch }}
          path: ${{ runner.temp }}/staged/*.tar.zst
          retention-days: 7
          if-no-files-found: error

      # Atomic per-matrix-entry publish: archive upload + index.toml
      # mutation under the state-lock acquired for the target tag.
      # Different release tags use different lock subjects (durable
      # binaries-abi-v<N> vs pr-<N>-staging), so concurrent rebuilds
      # to independent tags don't block each other.
      #
      # On failure: a separate step records status=failed in the
      # index so consumers see the rebuild attempt; if a prior
      # successful build exists, IndexToml::update_entry_failed
      # preserves it in the fallback_* slot (last-green semantics).
      - name: Publish archive + index entry
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          bash scripts/index-update.sh \
            --target-tag "${{ needs.preflight.outputs.target_tag }}" \
            --package "${{ matrix.package }}" \
            --version "${{ matrix.version }}" \
            --revision "${{ matrix.revision }}" \
            --arch "${{ matrix.arch }}" \
            --status success \
            --archive-path "${{ steps.build.outputs.archive-path }}" \
            --archive-name "${{ steps.build.outputs.archive-name }}" \
            --cache-key-sha "${{ matrix.sha }}"

      - name: Record failed build in index
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          bash scripts/index-update.sh \
            --target-tag "${{ needs.preflight.outputs.target_tag }}" \
            --package "${{ matrix.package }}" \
            --version "${{ matrix.version }}" \
            --revision "${{ matrix.revision }}" \
            --arch "${{ matrix.arch }}" \
            --status failed \
            --error "matrix-build failed; see ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            || true

  # Program wave — kind=program packages, gated on the library wave.
  matrix-build:
    needs: [preflight, toolchain-cache, promote-staging, lib-matrix-build]
    if: |
      !cancelled() &&
      needs.preflight.result == 'success' &&
      needs.toolchain-cache.result == 'success' &&
      (needs.promote-staging.result == 'success' || needs.promote-staging.result == 'skipped') &&
      (needs.lib-matrix-build.result == 'success' || needs.lib-matrix-build.result == 'skipped') &&
      needs.preflight.outputs.program_matrix != '[]'
    runs-on: ubuntu-latest
    permissions:
      contents: write    # state-lock git push + gh release create/upload
    env:
      WASM_POSIX_BINARY_INDEX_URL: https://github.com/${{ github.repository }}/releases/download/${{ needs.preflight.outputs.target_tag }}/index.toml
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        include: ${{ fromJSON(needs.preflight.outputs.program_matrix) }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Compute commit-bound build provenance
        id: provenance
        run: |
          ts=$(git log -1 --format=%aI HEAD)
          echo "build-timestamp=$ts" >> "$GITHUB_OUTPUT"
          echo "build-host=${{ github.repository }}@${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo "provenance: build-timestamp=$ts" >&2

      - name: Fetch musl submodule
        uses: ./.github/actions/fetch-submodules
        with:
          submodules: libc/musl

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22

      - name: Cache Nix store + flake eval
        uses: DeterminateSystems/magic-nix-cache-action@v13

      - name: Download toolchain sysroots artifact
        uses: actions/download-artifact@v4
        with:
          name: toolchain-sysroots
          path: ${{ runner.temp }}/toolchain

      - name: Extract toolchain sysroots
        run: |
          tar --zstd -xf "$RUNNER_TEMP/toolchain/toolchain-sysroots.tar.zst"

      - name: Install host npm deps (build prereq)
        run: |
          bash scripts/dev-shell.sh bash -c '
            PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
            cd host && PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
          '

      - name: Download library archives from lib-matrix-build
        if: needs.preflight.outputs.library_matrix != '[]'
        uses: actions/download-artifact@v4
        with:
          pattern: '*-wasm*'
          path: ${{ runner.temp }}/lib-archives

      - name: Materialize library overlays
        if: needs.preflight.outputs.library_matrix != '[]'
        run: |
          bash scripts/materialize-pr-overlays.sh "$RUNNER_TEMP/lib-archives"

      - name: Resolve target package's transitive deps
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo run --release -p xtask --target "$HOST_TARGET" --quiet -- \
              build-deps --arch "${{ matrix.arch }}" --binaries-dir "$(pwd)/binaries" \
                resolve "${{ matrix.package }}"
          '

      - name: Build ${{ matrix.package }} (${{ matrix.arch }})
        id: build
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo run --release -p xtask --target "$HOST_TARGET" --quiet -- \
              archive-stage \
                --package "packages/registry/${{ matrix.package }}" \
                --arch "${{ matrix.arch }}" \
                --out "$RUNNER_TEMP/staged" \
                --build-timestamp "${{ steps.provenance.outputs.build-timestamp }}" \
                --build-host "${{ steps.provenance.outputs.build-host }}"
          '
          archive_path=$(find "$RUNNER_TEMP/staged" -name '*.tar.zst' -print -quit)
          archive_name=$(basename "$archive_path")
          echo "archive-path=$archive_path" >> "$GITHUB_OUTPUT"
          echo "archive-name=$archive_name" >> "$GITHUB_OUTPUT"

      - name: Upload archive as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.package }}-${{ matrix.arch }}
          path: ${{ runner.temp }}/staged/*.tar.zst
          retention-days: 7
          if-no-files-found: error

      # Atomic per-matrix-entry publish — see lib-matrix-build for
      # the rationale.
      - name: Publish archive + index entry
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          bash scripts/index-update.sh \
            --target-tag "${{ needs.preflight.outputs.target_tag }}" \
            --package "${{ matrix.package }}" \
            --version "${{ matrix.version }}" \
            --revision "${{ matrix.revision }}" \
            --arch "${{ matrix.arch }}" \
            --status success \
            --archive-path "${{ steps.build.outputs.archive-path }}" \
            --archive-name "${{ steps.build.outputs.archive-name }}" \
            --cache-key-sha "${{ matrix.sha }}"

      - name: Record failed build in index
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          bash scripts/index-update.sh \
            --target-tag "${{ needs.preflight.outputs.target_tag }}" \
            --package "${{ matrix.package }}" \
            --version "${{ matrix.version }}" \
            --revision "${{ matrix.revision }}" \
            --arch "${{ matrix.arch }}" \
            --status failed \
            --error "matrix-build failed; see ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            || true

  test-gate:
    needs: [change-scope, preflight, toolchain-cache, promote-staging, lib-matrix-build, matrix-build]
    if: |
      !cancelled() &&
      needs.toolchain-cache.result == 'success' &&
      (
        (
          needs.change-scope.outputs.package_staging_required == 'true' &&
          needs.preflight.result == 'success' &&
          (needs.promote-staging.result == 'success' || needs.promote-staging.result == 'skipped') &&
          (needs.lib-matrix-build.result == 'success' || needs.lib-matrix-build.result == 'skipped') &&
          (needs.matrix-build.result == 'success' || needs.matrix-build.result == 'skipped')
        ) ||
        (
          needs.change-scope.outputs.package_staging_required == 'false' &&
          needs.change-scope.outputs.kernel == 'true'
        )
      )
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      merge_base_sha: ${{ steps.merge_ref.outputs.merge_base_sha }}
    env:
      PACKAGE_STAGING_REQUIRED: ${{ needs.change-scope.outputs.package_staging_required }}
      PACKAGE_TARGET_TAG: ${{ needs.preflight.outputs.target_tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          # Keep the synthetic pull-request merge commit's two parents
          # available. With the default depth=1 Git marks HEAD shallow,
          # so `git show --format=%P HEAD` reports no parents.
          fetch-depth: 2

      - name: Capture PR merge base
        id: merge_ref
        run: |
          set -euo pipefail
          parents=$(git show --no-patch --format=%P HEAD)
          MERGE_BASE_SHA=$(printf "%s\n" "$parents" | awk "{print \$1}")
          MERGE_HEAD_SHA=$(printf "%s\n" "$parents" | awk "{print \$2}")
          if [ -z "$MERGE_BASE_SHA" ] || [ -z "$MERGE_HEAD_SHA" ]; then
            echo "::error::prepare-merge expected HEAD to be the pull_request merge commit, got parents: $parents"
            exit 1
          fi
          if [ "$MERGE_HEAD_SHA" != "${{ github.event.pull_request.head.sha }}" ]; then
            echo "::error::pull_request merge ref second parent $MERGE_HEAD_SHA does not match PR head ${{ github.event.pull_request.head.sha }}"
            exit 1
          fi
          echo "merge_base_sha=$MERGE_BASE_SHA" >> "$GITHUB_OUTPUT"

      - name: Fetch submodules (musl, libc-test, os-test)
        uses: ./.github/actions/fetch-submodules
        with:
          submodules: libc/musl tests/libc/libc-test tests/sortix/os-test

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v18

      - name: Cache Nix store + flake eval
        uses: DeterminateSystems/magic-nix-cache-action@v13

      - name: Download toolchain sysroots artifact
        uses: actions/download-artifact@v4
        with:
          name: toolchain-sysroots
          path: ${{ runner.temp }}/toolchain

      - name: Extract toolchain sysroots
        run: |
          tar --zstd -xf "$RUNNER_TEMP/toolchain/toolchain-sysroots.tar.zst"

      - name: Download matrix artifacts
        if: ${{ needs.change-scope.outputs.package_staging_required == 'true' && (needs.preflight.outputs.library_matrix != '[]' || needs.preflight.outputs.program_matrix != '[]') }}
        uses: actions/download-artifact@v4
        with:
          pattern: '*-wasm*'
          path: ${{ runner.temp }}/staged-archives

      - name: Install host npm deps (resolver prereq)
        # Must run before Materialize binaries (see staging-build.yml).
        run: |
          bash scripts/dev-shell.sh bash -c '
            PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
            cd host && PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
          '

      - name: Materialize binaries
        env:
          WASM_POSIX_FETCH_SKIP_PKGS: cpython erlang erlang-vfs perl perl-vfs python-vfs redis ruby texlive
        # Package-staging runs resolve against the durable release plus
        # local file:// overlays for newly built/promoted archives. Kernel-only
        # runs skip overlays and resolve every package from the existing
        # durable ABI release; the fresh kernel built below shadows kernel.wasm
        # via local-binaries/.
        run: |
          set -euo pipefail
          if [ "$PACKAGE_STAGING_REQUIRED" = "true" ]; then
            export WASM_POSIX_BINARY_INDEX_URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/${PACKAGE_TARGET_TAG}/index.toml"
            STAGE="$RUNNER_TEMP/staged-overlay"
            bash scripts/materialize-pr-overlays.sh "$RUNNER_TEMP/staged-archives" "$STAGE"
          else
            ABI=$(grep -oE "ABI_VERSION: u32 = [0-9]+" crates/shared/src/lib.rs | awk '{print $4}')
            export WASM_POSIX_BINARY_INDEX_URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/binaries-abi-v${ABI}/index.toml"
          fi
          echo "WASM_POSIX_BINARY_INDEX_URL=$WASM_POSIX_BINARY_INDEX_URL" >> "$GITHUB_ENV"
          bash scripts/dev-shell.sh bash scripts/fetch-binaries.sh --allow-stale

      - name: Build kernel + test programs + install host deps
        # `npx playwright install chromium` mirrors staging-build.yml: the
        # wordpress-site-editor E2E test invokes `chromium.launch()`
        # directly, so the browser binary must be present in the runner
        # cache before vitest runs. `npm install` alone only fetches the
        # @playwright/test JS package, not the browser.
        run: |
          bash scripts/dev-shell.sh bash -c '
            set -euo pipefail
            cargo build --release -p wasm-posix-kernel \
              -Z build-std=core,alloc
            mkdir -p local-binaries
            cp target/wasm64-unknown-unknown/release/wasm_posix_kernel.wasm \
              local-binaries/kernel.wasm
            bash scripts/build-programs.sh
            PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund
            cd host && PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci --no-audit --no-fund && cd ..
            bash scripts/build-rootfs.sh
            npx --prefix host playwright install chromium
          '

      - name: Run cargo tests (kernel unit tests)
        if: needs.change-scope.outputs.kernel == 'true'
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo test -p wasm-posix-kernel --target "$HOST_TARGET" --lib
          '

      - name: Run fork-instrument tests
        if: needs.change-scope.outputs.kernel == 'true'
        run: |
          bash scripts/dev-shell.sh bash -c '
            HOST_TARGET=$(rustc -vV | awk "/^host/ {print \$2}")
            cargo test -p fork-instrument --target "$HOST_TARGET"
          '

      - name: Run vitest (host integration tests)
        run: |
          bash scripts/dev-shell.sh bash -c '
            cd host && npx vitest run
          '

      - name: Run libc-test conformance suite
        if: needs.change-scope.outputs.kernel == 'true'
        run: |
          bash scripts/dev-shell.sh bash scripts/run-libc-tests.sh

      - name: Run Open POSIX Test Suite
        if: needs.change-scope.outputs.kernel == 'true'
        run: |
          bash scripts/dev-shell.sh bash scripts/run-posix-tests.sh

      - name: Run Sortix os-test suite
        if: needs.change-scope.outputs.kernel == 'true'
        run: |
          bash scripts/dev-shell.sh bash scripts/run-sortix-tests.sh --all

  # publish / generate-index / amend-package-toml — REMOVED.
  #
  # Per the binary-resolution-via-index-ledger design, each
  # matrix-build entry now publishes its archive AND mutates
  # index.toml atomically under the workflow-level state-lock
  # (see the "Publish archive + index entry" step in
  # lib-matrix-build / matrix-build above). The three former jobs
  # — separate publish, generate-index, and amend-package-toml —
  # are no longer needed:
  #
  #   * publish: per-entry steps upload to the release directly.
  #   * generate-index: index.toml is mutated per-entry by
  #     scripts/index-update.sh; no separate aggregation pass.
  #   * amend-package-toml: the index.toml ledger IS the consumer-
  #     visible state. There are no [binary] URLs in package.toml
  #     to amend; the bot-PR mechanism is gone.


  # ---------------------------------------------------------------------
  # merge-gate-post-noop — package staging was not required.
  #
  # Browser/docs/workflow-only PRs do not need PR staging archives and
  # must not trigger durable package builds on label. After the regular
  # review/check/mergeability gate succeeds, post the same merge-gate
  # success context and enable auto-merge without touching releases.
  # ---------------------------------------------------------------------
  merge-gate-post-noop:
    needs: [gate, change-scope]
    if: |
      !cancelled() &&
      needs.gate.result == 'success' &&
      needs.change-scope.outputs.package_staging_required == 'false' &&
      needs.change-scope.outputs.kernel == 'false'
    runs-on: ubuntu-latest
    permissions:
      contents: write          # gh pr merge --auto (enablePullRequestAutoMerge)
      pull-requests: write     # auto-merge + comment
      statuses: write          # post merge-gate=success
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Post merge-gate=success on original PR HEAD
        run: |
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "/repos/${{ github.repository }}/statuses/$PR_HEAD_SHA" \
            -f state=success \
            -f context=merge-gate \
            -f description="No package-affecting changes; package staging skipped."

      - name: Enable squash auto-merge on original PR
        id: automerge
        run: |
          set +e
          output=$(gh pr merge "$PR_NUMBER" \
            --repo ${{ github.repository }} \
            --auto --squash 2>&1)
          status=$?
          set -e

          if [ "$status" -eq 0 ]; then
            printf '%s\n' "$output"
            echo "manual_merge_required=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          printf '%s\n' "$output" >&2
          if printf '%s\n' "$output" | grep -q 'without `workflows` permission'; then
            echo "manual_merge_required=true" >> "$GITHUB_OUTPUT"
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge passed and posted \`merge-gate=success\`, but GitHub Actions cannot enable auto-merge because this PR changes workflow files. A user with workflow permission must merge this PR manually."
            exit 0
          fi

          echo "manual_merge_required=false" >> "$GITHUB_OUTPUT"
          exit "$status"

      - name: Comment on original PR
        continue-on-error: true
        run: |
          if [ "${{ steps.automerge.outputs.manual_merge_required }}" = "true" ]; then
            BODY="prepare-merge: no package-affecting changes; package staging and durable package publishing were skipped. \`merge-gate=success\` posted on PR HEAD. Manual merge is required because GitHub Actions could not enable auto-merge for this workflow-changing PR."
          else
            BODY="prepare-merge: no package-affecting changes; package staging and durable package publishing were skipped. \`merge-gate=success\` posted on PR HEAD and auto-merge enabled."
          fi
          set +e
          for attempt in 1 2 3; do
            if gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "$BODY"; then
              exit 0
            fi
            echo "::warning::gh pr comment failed (attempt $attempt/3); retrying in 10s"
            sleep 10
          done
          echo "::warning::gh pr comment failed after 3 attempts; merge-gate was posted but the explanatory comment was not"

      - name: Drop label + comment on failure
        if: failure()
        run: |
          gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship || true
          gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge failed during the no-op merge-gate-post step. Check the workflow run log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}. Re-apply the ready-to-ship label after fixing." || true

  # ---------------------------------------------------------------------
  # merge-gate-post-kernel-only — package staging was not required, but
  # kernel/runtime tests were.
  #
  # Non-ABI kernel/runtime PRs should validate the PR merge ref without
  # building or publishing package archives. Once test-gate passes against
  # durable binaries plus the freshly built kernel, post the normal
  # merge-gate context and enable auto-merge.
  # ---------------------------------------------------------------------
  merge-gate-post-kernel-only:
    needs: [gate, change-scope, test-gate]
    if: |
      !cancelled() &&
      needs.gate.result == 'success' &&
      needs.test-gate.result == 'success' &&
      needs.change-scope.outputs.package_staging_required == 'false' &&
      needs.change-scope.outputs.kernel == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write          # gh pr merge --auto (enablePullRequestAutoMerge)
      pull-requests: write     # auto-merge + comment
      statuses: write          # post merge-gate=success
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Verify prepared base is still current
        run: |
          set -euo pipefail
          PREPARED_BASE="${{ needs.test-gate.outputs.merge_base_sha }}"
          if [ -z "$PREPARED_BASE" ]; then
            echo "::error::test-gate did not report merge_base_sha"
            exit 1
          fi

          BASE_REF="${{ github.event.pull_request.base.ref }}"
          git init --quiet "$RUNNER_TEMP/base-drift"
          cd "$RUNNER_TEMP/base-drift"
          git remote add origin "https://${{ github.token }}@github.com/${{ github.repository }}.git"
          git fetch --quiet --depth=1 origin "$BASE_REF:refs/remotes/origin/$BASE_REF"
          CURRENT_BASE=$(git rev-parse "refs/remotes/origin/$BASE_REF")

          if [ "$CURRENT_BASE" != "$PREPARED_BASE" ]; then
            gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: \`$BASE_REF\` advanced while prepare-merge was running (prepared \`$PREPARED_BASE\`, current \`$CURRENT_BASE\`). Re-apply \`ready-to-ship\` so kernel/runtime tests run against the latest base."
            exit 1
          fi

      - name: Post merge-gate=success on original PR HEAD
        run: |
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "/repos/${{ github.repository }}/statuses/$PR_HEAD_SHA" \
            -f state=success \
            -f context=merge-gate \
            -f description="Kernel test-gate passed; package staging and publishing skipped."

      - name: Enable squash auto-merge on original PR
        id: automerge
        run: |
          set +e
          output=$(gh pr merge "$PR_NUMBER" \
            --repo ${{ github.repository }} \
            --auto --squash 2>&1)
          status=$?
          set -e

          if [ "$status" -eq 0 ]; then
            printf '%s\n' "$output"
            echo "manual_merge_required=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          printf '%s\n' "$output" >&2
          if printf '%s\n' "$output" | grep -q 'without `workflows` permission'; then
            echo "manual_merge_required=true" >> "$GITHUB_OUTPUT"
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge passed and posted \`merge-gate=success\`, but GitHub Actions cannot enable auto-merge because this PR changes workflow files. A user with workflow permission must merge this PR manually."
            exit 0
          fi

          echo "manual_merge_required=false" >> "$GITHUB_OUTPUT"
          exit "$status"

      - name: Comment on original PR
        continue-on-error: true
        run: |
          if [ "${{ steps.automerge.outputs.manual_merge_required }}" = "true" ]; then
            BODY="prepare-merge: kernel/runtime tests passed against the PR merge ref; package staging and durable package publishing were skipped. \`merge-gate=success\` posted on PR HEAD. Manual merge is required because GitHub Actions could not enable auto-merge for this workflow-changing PR."
          else
            BODY="prepare-merge: kernel/runtime tests passed against the PR merge ref; package staging and durable package publishing were skipped. \`merge-gate=success\` posted on PR HEAD and auto-merge enabled."
          fi
          set +e
          for attempt in 1 2 3; do
            if gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "$BODY"; then
              exit 0
            fi
            echo "::warning::gh pr comment failed (attempt $attempt/3); retrying in 10s"
            sleep 10
          done
          echo "::warning::gh pr comment failed after 3 attempts; merge-gate was posted but the explanatory comment was not"

      - name: Drop label + comment on failure
        if: failure()
        run: |
          gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship || true
          gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge failed during the kernel-only merge-gate-post step. Check the workflow run log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}. Re-apply the ready-to-ship label after fixing." || true

  # ---------------------------------------------------------------------
  # merge-gate-post — single merge-gate poster + auto-merge enabler.
  #
  # Post binary-resolution-via-index-ledger: there's no bot PR (no
  # in-tree URLs to amend), and per-matrix-entry atomic
  # archive+index publish happens inline. This job posts
  # merge-gate=success on the ORIGINAL PR's HEAD SHA and enables
  # auto-merge there.
  #
  # Fires when test-gate succeeds against the durable release state.
  # Prepare may have built missing archives, promoted exact archives
  # from pr-<N>-staging, or found every cache_key_sha already on the
  # durable tag; in all cases test-gate validates the PR merge ref
  # against what consumers will fetch from binaries-abi-v<N>.
  # ---------------------------------------------------------------------
  merge-gate-post:
    needs: [gate, change-scope, preflight, test-gate]
    if: |
      !cancelled() &&
      needs.change-scope.outputs.package_staging_required == 'true' &&
      needs.preflight.result == 'success' &&
      needs.test-gate.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write          # gh pr merge --auto (enablePullRequestAutoMerge)
      pull-requests: write     # auto-merge + comment
      statuses: write          # post merge-gate=success
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Verify prepared base is still current
        run: |
          set -euo pipefail
          PREPARED_BASE="${{ needs.preflight.outputs.merge_base_sha }}"
          if [ -z "$PREPARED_BASE" ]; then
            echo "::error::preflight did not report merge_base_sha"
            exit 1
          fi

          BASE_REF="${{ github.event.pull_request.base.ref }}"
          git init --quiet "$RUNNER_TEMP/base-drift"
          cd "$RUNNER_TEMP/base-drift"
          git remote add origin "https://${{ github.token }}@github.com/${{ github.repository }}.git"
          git fetch --quiet --depth=1 origin "$BASE_REF:refs/remotes/origin/$BASE_REF"
          CURRENT_BASE=$(git rev-parse "refs/remotes/origin/$BASE_REF")

          if [ "$CURRENT_BASE" != "$PREPARED_BASE" ]; then
            gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "Cannot prepare merge: \`$BASE_REF\` advanced while prepare-merge was running (prepared \`$PREPARED_BASE\`, current \`$CURRENT_BASE\`). Re-apply \`ready-to-ship\` so packages are validated against the latest base."
            exit 1
          fi

      - name: Post merge-gate=success on original PR HEAD
        run: |
          DESC="test-gate passed on the PR merge ref against durable archives on ${{ needs.preflight.outputs.target_tag }}."
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "/repos/${{ github.repository }}/statuses/$PR_HEAD_SHA" \
            -f state=success \
            -f context=merge-gate \
            -f description="$DESC"

      - name: Enable squash auto-merge on original PR
        id: automerge
        run: |
          set +e
          output=$(gh pr merge "$PR_NUMBER" \
            --repo ${{ github.repository }} \
            --auto --squash 2>&1)
          status=$?
          set -e

          if [ "$status" -eq 0 ]; then
            printf '%s\n' "$output"
            echo "manual_merge_required=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          printf '%s\n' "$output" >&2
          if printf '%s\n' "$output" | grep -q 'without `workflows` permission'; then
            echo "manual_merge_required=true" >> "$GITHUB_OUTPUT"
            gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge passed and posted \`merge-gate=success\`, but GitHub Actions cannot enable auto-merge because this PR changes workflow files. A user with workflow permission must merge this PR manually."
            exit 0
          fi

          echo "manual_merge_required=false" >> "$GITHUB_OUTPUT"
          exit "$status"

      - name: Comment on original PR
        # Purely informational — the merge already succeeded by this
        # point. A transient `gh pr comment` failure (e.g. GitHub API
        # 504) must NOT trigger the `if: failure()` handler below,
        # which would post a misleading "merge-gate failed; re-apply
        # ready-to-ship" message and remove the label from an
        # already-merged PR. continue-on-error: true contains the
        # blast radius; the inner retry recovers most transients.
        continue-on-error: true
        run: |
          if [ "${{ steps.automerge.outputs.manual_merge_required }}" = "true" ]; then
            BODY="prepare-merge: test-gate passed against the PR merge ref and [\`${{ needs.preflight.outputs.target_tag }}\`](https://github.com/${{ github.repository }}/releases/tag/${{ needs.preflight.outputs.target_tag }}). Missing entries were built or promoted from PR staging only when their merged-tree cache key matched; \`merge-gate=success\` posted on PR HEAD. Manual merge is required because GitHub Actions could not enable auto-merge for this workflow-changing PR."
          else
            BODY="prepare-merge: test-gate passed against the PR merge ref and [\`${{ needs.preflight.outputs.target_tag }}\`](https://github.com/${{ github.repository }}/releases/tag/${{ needs.preflight.outputs.target_tag }}). Missing entries were built or promoted from PR staging only when their merged-tree cache key matched; \`merge-gate=success\` posted on PR HEAD and auto-merge enabled."
          fi
          set +e
          for attempt in 1 2 3; do
            if gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "$BODY"; then
              exit 0
            fi
            echo "::warning::gh pr comment failed (attempt $attempt/3); retrying in 10s"
            sleep 10
          done
          echo "::warning::gh pr comment failed after 3 attempts; PR is already merged but the explanatory comment was not posted"

      - name: Drop label + comment on failure
        if: failure()
        run: |
          gh pr edit "$PR_NUMBER" --repo ${{ github.repository }} --remove-label ready-to-ship || true
          gh pr comment "$PR_NUMBER" --repo ${{ github.repository }} --body "prepare-merge failed during the merge-gate-post step. Check the workflow run log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}. Re-apply the ready-to-ship label after fixing." || true
